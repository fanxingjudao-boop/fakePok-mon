# ポケットレジェンド 碧環の旅

自然循環をささえる古代装置「碧環(へきかん)」が弱まった **セイリュウ地方** を舞台に、
守護獣と信頼を結び、失われた4つの環核を再接続する ―― オリジナルのモンスター共生RPG。
モンスター・楽曲・ドット絵はすべてコード生成で、外部アセットに一切依存しません(完全オフライン)。

## あそびかた

`index.html` をブラウザで開くだけで遊べます(ビルド不要・オフライン動作)。

| 操作 | キーボード | 画面ボタン |
|---|---|---|
| 移動 | 矢印キー / WASD | 十字キー |
| けってい・はなす | Z / Space | A |
| キャンセル | X / Esc | B |
| メニュー(ずかん/モンスター/バッグ/レポート) | Enter | START |

## 物語と世界(碧環の旅)

主人公は「選ばれた特別な少年」ではなく、地域の異変を記録する **若き巡環士候補**。
モンスターを倒して支配するのではなく、各地の **守護獣と信頼を築き** 環核を取り戻す。
ライバル **レン** は単純な悪役ではなく「人が装置を制御すべき」という合理主義者で、
主人公の「生態系に選択を委ねる」方針と対立する。

**非線形ワールド(一本道を廃止)** ― 中央拠点コダチから複数地域を任意の順で攻略:

```
コダチ拠点 ─┬─ 碧樹圏(森) ─┐
            └─ 潮環圏(水) ─┴→ 中央遺構 ─┬─ 火脈圏(炎/岩) ─┐
                                          └─ 雷霧圏(電/飛) ─┴→ 碧環中枢(結末)
```

- **環核試練**(8ジムを廃止し4地域に統合): 各地の守護獣に挑み環核を得る。2環核で中央遺構、
  4環核＋レンとの決着で碧環中枢が開く(**早期侵入不可**)。
- **能力ゲート**: フィールド能力は種族ではなく **タイプ** で判定(複数解決)。ほのお=倒木、
  みず=水門、いわ/みず=岩、ひこう/でんき=崖。奥に **任意エリアと隠し宝**(再訪の理由)。
- **サブクエスト9件**: 追跡/収集/選択/救助/環境変化を混ぜ、選択は結末スコアに反映。
- **3方針の分岐エンディング**: 行動履歴から **完全復旧 / 自然循環 / 分散管理** を導出。
- **戦闘**: ポケモン式ターン制(わざPP/8タイプ相性/急所/状態異常/交代/捕獲/経験値/しんか、
  踏み込み・タイプ色ヒット・画面シェイク演出)。**モンスターは28種を32×32骨格別に描画**。
- **音楽**: Web Audio チップチューン。**ボックス**(センターで手持ち⇔預り)、レポート(セーブv2)。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 画面シェル・DOM構造 |
| `styles.css` | UIスタイル(システムフォント・オフライン) |
| `audio.js` | チップチューン音楽エンジン+楽曲+SE |
| `world-maps.js` | **碧環の旅の固有マップ**(拠点/4地域/中央遺構/中枢・全マップ一意) |
| `data.js` | タイプ相性・わざ・図鑑(28種)・通行判定(requirements/能力/環核) |
| `story.js` | 世界グラフ・ストーリーフラグ・地域解放・スケーリング・分岐エンディング |
| `sprites.js` | タイル・歩行キャラ描画 |
| `monster-art.js` | 28種の32×32骨格別モンスター描画（前面/背面） |
| `battle.js` | バトルエンジン |
| `game.js` | オーバーワールド・UI・ストーリー進行(試練/レン/サブクエ/結末)・セーブ |
| `CLAUDE_RENEWAL_BRIEF.md` | 刷新の依頼書(仕様と完了条件) |

## 開発時の確認

```bash
node --check world-maps.js data.js story.js game.js
node tests/monster-art-smoke.cjs   # 28種の前面/背面スプライト生成
node tests/map-validate.cjs        # 出入口接続・歩行可能性・到達性・行幅・rows一意(共有は失敗)
node tests/world-graph.cjs         # 世界グラフ・地域解放・早期侵入防止・エンディング
node tests/progression.cjs         # 起点/遺構/中枢ゲート・能力ゲート・試練4種・サブクエ・結末
```

セーブは `fakemon_save_v2`(旧 v1 は自動移行)。破損セーブでも起動可能。

## 刷新の進捗(CLAUDE_RENEWAL_BRIEF.md 対応)

基盤スタッフを実装済み:

- **完全オフライン**: Google Fonts への外部接続を廃止し、OS 同梱の丸/角ゴシックへフォールバック。
- **セーブデータ v2**: `fakemon_save_v2` に保存。旧 `fakemon_save_v1` を自動でマイグレーション。
  欠損・破損セーブでも起動不能にならないよう正規化(不正な現在地は回復地点へ退避)。
- **ボックス機能**: モンスターセンターに「ボックス」端末。手持ち⇔ボックスの預け／引き出し／一覧・詳細。
- **通行ゲートの共通化**: 出入口ごとの `requirements` を `GameData.meetsRequirements()` で共通判定。
  ヴィクトリーロードは全 8 バッジ判定へ修正。ミナモ→ルート2 を NPC 依存でないエッジ条件化(迂回不可)。
- **マップ検証**: `tests/map-validate.cjs` で接続・歩行・到達性を検査(`rows` 共有マップは固有化対象として警告)。

後続(地域・物語の本体刷新)は `CLAUDE_RENEWAL_BRIEF.md` の実装順に沿って進行:
固有マップ4地域＋中央遺構、環核試練、サブクエスト8件、旅記録／マップUI、分岐エンディング。
